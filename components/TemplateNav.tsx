"use client";
import { useEffect, useState } from "react";
const links=[['Story','#story'],['Teas','#teas'],['Craft','#craft'],['Golden Pearl','#golden-pearl'],['For Buyers','#buyers'],['Contact','#contact']];
export function TemplateNav(){const [open,setOpen]=useState(false);useEffect(()=>{const escape=(event:KeyboardEvent)=>{if(event.key==='Escape')setOpen(false)};addEventListener('keydown',escape);document.body.classList.toggle('menu-open',open);return()=>{removeEventListener('keydown',escape);document.body.classList.remove('menu-open')}},[open]);return <header className="th-nav"><div className="th-container th-nav-inner"><a href="#home" className="th-logo"><span>AFT</span><strong>TECHNO TRADE</strong><small>AFT TEA FACTORY</small></a><button aria-label={open?'Close navigation':'Open navigation'} aria-expanded={open} aria-controls="mobile-navigation" onClick={()=>setOpen(!open)} className="th-toggle"><i/><i/></button><nav id="mobile-navigation" className={open?'open':''}>{links.map(([t,h])=><a href={h} onClick={()=>setOpen(false)} key={h}>{t}</a>)}<a className="th-nav-enquire" href="#contact" onClick={()=>setOpen(false)}>Enquire Now <b>â†—</b></a></nav></div></header>}
